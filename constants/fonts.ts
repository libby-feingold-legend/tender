export const fonts = {
  serif: 'Georgia',       // system serif fallback (replace with Playfair Display via expo-font later)
  mono: 'Courier New',   // system mono fallback (replace with Courier Prime via expo-font later)
  sans: 'System',        // system sans (Inter on iOS/Android)
};
